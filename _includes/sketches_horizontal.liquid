<div class="col mb-4">
  <a href="{% if sketch.redirect %}{{ sketch.redirect }}{% else %}{{ sketch.url | relative_url }}{% endif %}">
    <div class="card h-100 hoverable">
      <div class="row no-gutters">
        {% if sketch.img %}
          <div class="col-md-6">
            {% include figure.liquid loading="eager" path=sketch.img sizes="(min-width: 768px) 156px, 50vw" alt="project thumbnail" class="card-img" %}
          </div>
        {% endif %}
        <div class="{% if sketch.img %}col-md-6{% else %}col-md-12{% endif %}">
          <div class="card-body">
            <h3 class="card-title">{{ sketch.title }}</h3>
            <p class="card-text">{{ sketch.description }}</p>
            <div style="display:none;">{{ sketch.category }}</div>
            <div class="row ml-1 mr-1 p-0">
              {% if sketch.github %}
                <div class="github-icon">
                  <div class="icon" data-toggle="tooltip" title="Code Repository">
                    <a href="{{ sketch.github }}"><i class="fa-brands fa-github gh-icon"></i></a>
                  </div>
                  {% if sketch.github_stars %}
                    <span class="stars" data-toggle="tooltip" title="GitHub Stars">
                      <i class="fa-solid fa-star"></i>
                      <span id="{{ sketch.github_stars }}-stars"></span>
                    </span>
                  {% endif %}
                </div>
              {% endif %}
            </div>
          </div>
        </div>
      </div>
    </div>
  </a>
</div>
